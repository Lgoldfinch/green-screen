db {
      port = "5432"
      port = ${?DB_PORT}
      host = "localhost"
      host = ${?DB_URL}
      db-user = "postgres"
      db-user = ${?DB_USER}
      password = "password"
      password = ${?DB_PASSWORD}
      name = "green-screen-postgres"
      name = ${?DB_NAME}
      pool-size = 16
      pool-size = ${?DB_POOL_SIZE}
    }